function navigateBack(page) {
    window.location.href = page; // Redirect to the specified page
}

function navigateToAddRoom() {
    window.location.href = "addmultiroom.html"; // Redirects to Add Room page
}